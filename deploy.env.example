# Copy to `.env.deploy` in this folder (gitignored) or set the same
# variables in your shell. Real env always wins over this file.
#
#   copy deploy.env.example .env.deploy
#
WBTI_DEPLOY_REMOTE=https://github.com/fingerfly/wbti.git

# Optional:
# WBTI_DEPLOY_DIR=C:\temp\wbti-deploy
# WBTI_DEPLOY_GIT_NAME=Your Name
# WBTI_DEPLOY_GIT_EMAIL=you@example.com
